{% extends 'base.html.twig' %}

{% block body %}
{% if app.user is not null %}
<h1>{{ 'compte.Wel'|trans }} {{ app.user.nom }} {{ app.user.prenom }}</h1>
     {% for commande in commandes %}
        <h3 style="color:white">{{ 'Commande.CommandeNumber'|trans }} : {{ commande.id }}</h3>
        <h3 style="color:white">{{ 'Commande.DateCommande'|trans }} : {{ commande.date|date('Y-m-d') }}</h3>
    {% endfor %}
    <table class="table" border="1">
        <thead>
            <tr>
                <th>{{ 'Commande.NomProduit'|trans }}</th>
                <th>{{ 'Commande.QuantiteCommande'|trans }}</th>
            </tr>
       </thead>
    <tbody>
     {% for commande in commandes %}
        <tr>
            <td>{{ commande.produit }}</td>
            <td>{{ commande.quantite }}</td>
        </tr>
        {% endfor %}
    </tbody>
 </table>
    {% for commande in commandes %}
        <h3 style="color:white">{{ 'Commande.TotalCommande'|trans }} : {{ commande.montant }} €</h3>
    {% endfor %}
 <hr>

{% else %}
        <p>{{ 'app.PleaseConnect'|trans }}</p>
{% endif %}
{% endblock %}
