{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div style="text-align:center; margin-right:250px; margin-left:250px">
    <h1>{{ 'accueil.Inscription'|trans }}</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.nom) }}
            <br>
        {{ form_row(registrationForm.prenom) }}
            <br>
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
            <br>
        {{ form_row(registrationForm.email) }}
            <br>
        </div>
        <div style="text-align:center; margin-right:350px; margin-left:350px">
        {{ form_row(registrationForm.agreeTerms) }}
            <br>
        <button class="btn btn-success" type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
</div>
{% endblock %}
